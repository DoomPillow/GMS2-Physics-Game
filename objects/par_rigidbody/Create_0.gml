/// @description

array_push(controller.BODIES, id);

components = [];
position = new vector(x, y);
inv_mass = 0;
inertia = 0;
inv_inertia = 0;

velocity = new vector(vx, vy);
accel = new vector(0, 0);
acceleration = 1;
angular_velocity = 0;
fric = 0.03;